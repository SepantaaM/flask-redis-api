version: "3.9"

services:
  redis:
    image: redis
    restart: always
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
  flask:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - redis
    env_file:
      - .env

volumes:
  redis_data:
